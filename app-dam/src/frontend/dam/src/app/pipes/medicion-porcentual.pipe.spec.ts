import { MedicionPorcentualPipe } from './medicion-porcentual.pipe';

describe('MedicionPorcentualPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicionPorcentualPipe();
    expect(pipe).toBeTruthy();
  });
});
